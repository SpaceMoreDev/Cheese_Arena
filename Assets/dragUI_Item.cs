using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;
using Managers;
using Unity.VisualScripting;

public class dragUI_Item : MonoBehaviour
{
    GameObject _UIItem;
    GameObject _Temp;

    void Update()
    {
        // Vector2 ray = Camera.main.ScreenToViewportPoint(PlayerInputHandler.current.MouseVector);
        // RaycastHit2D hit;

        // // Perform the raycast
        // if (Physics2D.BoxCast(ray, new Vector2(1,1), 0, Vector2.zero))
        // {
        //     Debug.Log("is touching something");
        // }
    }
}
